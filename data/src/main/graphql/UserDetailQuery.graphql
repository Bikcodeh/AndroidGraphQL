query UserDetailQuery($id: String!) {
    user(id: $id) {
        id
        name
        profession
        age
        posts {
            id
            comment
        }
        hoobies {
            id
            title
            description
        }
    }
}