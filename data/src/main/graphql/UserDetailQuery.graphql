query UserDetailQuery($id: String!) {
    user(id: $id) {
        name
        posts {
            id
            comment
        }
        hoobies {
            id
            title
            description
        }
    }
}